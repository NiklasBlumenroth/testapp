// Beispielseiten für "Maschine", "Karte" und "Ausbau"
import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class MaschinePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Maschine')),
      body: Center(child: Text('Maschine Seite')),
    );
  }
}